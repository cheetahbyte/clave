meta {
  name: validate
  type: http
  seq: 2
}

post {
  url: {{host}}/api/{{version}}/validate
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer <activation-jwt>
}

body:json {
  {
    "deviceId": "apfelmus3"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
