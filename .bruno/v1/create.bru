meta {
  name: create
  type: http
  seq: 3
}

post {
  url: {{host}}/api/{{version}}/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{ADMIN_BEARER_TOKEN}}
}

body:json {
  {
    "productId": 1,
    "maxActivations": 500
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
